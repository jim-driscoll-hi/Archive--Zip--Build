Archive::Zip::Build

A module for creating (writing) zip data, in stages. For more information,
please see the POD documentation in the module itself.


BUILDING A TARBALL

To make a tar of this module do:

git archive --format tar -o Archive-Zip-Build-0.1.1.tar \
  --prefix Archive-Zip-Build-0.1.1/ HEAD
